import React from "react";

import './style.css';

 
// used in both sidedrawer & toolbar
const NavItem = (props) => {
    return(
        <div>
            NavItem this `un
        </div>
    );
}

export default NavItem;