import React from "react";

import './style.css';

const Nav = (props) => {
    return (
        <header className='Navbar'>
            <div>MENU</div>
            <div>LOGO</div>

            <nav>
                ...
            </nav>
        </header>
    );
}

export default Nav;